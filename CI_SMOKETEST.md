# Xcode Cloud pipeline smoke test

Throwaway PR to verify the Xcode Cloud workflow end to end:
clone → resolve packages → build → test → archive → TestFlight (internal).

Safe to close without merging. Delete this file once the pipeline is green.
