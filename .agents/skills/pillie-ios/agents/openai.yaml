interface:
  display_name: "Pillie iOS"
  short_description: "Build, run, debug, and visually verify the Pillie iOS app."
  default_prompt: "Follow the Pillie iOS loop: worktree, make build-and-run, visual QA."
